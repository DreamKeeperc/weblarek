# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### События
* Список всех событий, которые могут генерироваться в системе:
* События изменения данных (генерируются классами  моделями данных) 
- `basket:change` - изменение массива карточек внутри корзины; 
- `buyer:change` - изменение способа оплаты данных пользователя;
- `catalog:change` - изменение массива карточек;
- `card:save` - сохранение карточки для подробного отображения;

* События, возникающие при взаимодействии пользователя с интерфейсом (генерируются классами, отвечающими за представление)
- `modal:close` - событие на закрытие карточки внутри модального окна (событие генерируется при клике на крестик, нажатие на клавишу Esc, на клик по оверлею) 
- `order:continue` - событие на отправку на главный экран для начала новой сессии (событие генерируется при клике на кнопку `За новыми покупами`) 
- `cardPreview:buy` - событие на покупку карточки в превью (событие генерируется на клик по кнопке купить);
- `basket:open` - событие на открытие корзины в шапке(событие генерируется на нажатие на картинку корзины)
- `cardBasket:delete` - событие на удаление карточки из корзины (событие генерируется на нажатие кнопки удаления из корзины)
- `basket:continue` - событие на продолжение покупки товара(-ов) из корзины (событие генерируется на нажатие кнопки оформить в корзине)
- `form-order:payment` - событие на изменение кнопки для оплаты товаров (событие генерируется на нажатие одной из двух кнопок в корзине)
- `form-order:address` - событие на изменение инпута адреса доставки в форме заказа (событие генерируется при изменении поля ввода инпута)
- `form[name]:submit` - событие на отправку формы (событие генерируется при нажатии на кнопку 'Далее') 
- `form-contacts-email:input` - событие на изменение инпута email в форме заказа (событие генерируется при изменении поля ввода инпута)
- `form-contacts-phone:input` - событие на изменение инпута phone в форме заказа (событие генерируется при изменении поля ввода инпута)

### Данные
В приложении используются 2 сущности, которые описывают данные продукта: это продукт и покупатель.

#### Интерфейс IProduct
Внутри интефейса IProduct будет описана структура данных, которая будет использоваться дальше при проектировании класса карточки продукта.

```
interface IProduct {
  id: string; 
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null; 
}  
```

#### Интерфейс IBuyer
Внутри интефейса IBuyer будет описана структура данных, которая будет использоваться дальше при проектировании класса покупателя.

```
interface IBuyer {
  payment: TPayment; 
  email: string;
  phone: string;
  address: string;
}
```

```
type TPayment - это псевдоним типа, который хранит в себе конкретные строки: 'card' | 'cash' | '';
```

### Модели данных 

#### Класс Сatalog
Содержит в себе информацию о хранении карточек продуктов и их функциональности. 
Хранит в себе массив всех карточек продуктов.
Хранит выбранный продукт для подробного отображения.

Конструктор:  
`constructor(events: IEvents)` - в конструктор передается инстант класса брокера событий.

Поля класса:  
1) `protected products: IProduct[]` - массив продуктов, защищенное свой-во, чтобы к нему нельзя было обратиться вне класса.
2) `protected selectedProduct: IProduct | null = null` - один выбранный элемент массива. Также защищенное свой-во.
3) `protected events: IEvents` - создаем поле класса, в котором будем хранить брокер событий.

Методы:  
1) `setProducts(products: IProduct[])` - сохранение массива товаров полученного в параметрах метода;
2) `getProducts(): IProduct[]` - получение массива товаров из модели;
3) `getProductById(id: string): IProduct | null` - получение одного товара по его id. Может вернуть null в случае не нахождения по заданному id;
4) `setProduct(id: string)` - сохранение товара, переданного в параметры метода для подробного отображения;
5) `getProduct(): IProduct | null` - получение товара для подробного отображения. Может вернуть null в случае не нахождения по заданному id;

#### Класс Basket 
Содержит в себе информацию о хранении карточек продуктов и их функциональности. 
Хранит массив товаров, выбранных покупателем для покупки.

Конструктор:  
`constructor(events: IEvents)` - в конструктор передается инстант класса брокера событий.

Поля класса:  
1) `_products: IProduct[]` - массив выбранных продуктов;
2) `protected events: IEvents` - создаем поле класса, в котором будем хранить брокер событий.

Методы:  
1) `getProducts(): IProduct[]` - получение массива товаров, которые находятся в корзине; 
2) `addProduct(product: IProduct): void` - добавление товара, который был получен в параметре, в массив корзины;
3) `removeProduct(product: IProduct): void` - удаление товара, полученного в параметре из массива корзины;
4) `clear(): void` - очистка корзины;
5) `getTotalPrice(): number | null` - получение стоимости всех товаров в корзине;
6) `getCount(): number | string` - получение количества товаров в корзине;
7) `checkProductByID(id: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода.

#### Класс Buyer 
Данные покупателя, которые тот должен указать при оформлении заказа.

Конструктор:
`constructor(events: IEvents)` - в конструктор передается инстант класса брокера событий.

Поля класса:
1) `_payment: TPayment` - поле, хранящее способ оплаты покупателя;
2) `_address: string` - поле, хранящее адрес доставки покупателя;
3) `_phone: string` - поле, хранящее номер телефона покупателя;
4) `_email: string` - поле, хранящее емэйл покупателя;
5) `protected events: IEvents` - создаем поле класса, в котором будем хранить брокер событий.

Методы: 
1) `setPayment(payment: TPayment)` - сохранение способа платежа в моделе;
2) `setAddress(address: string)` - сохранение адреса доставки в моделе;
3) `setPhone(phone: string)` - сохранение номера телефона в моделе;
4) `setEmail(email: string)` - сохранение емэйла в моделе;
5) `getData(): IBuyer` - получение всех данных покупателя;
6) `clearData(): void` - очистка данных покупателя;
7) `isValid(): Errors` - метод валидации данных, который возвращает тип объекта с ошибками. В начале создается пустой объект и если поле не проходит валидацию, то ему присваивается строка с ошибкой. В типе Errors все поля опциональные. Если в поле корректные данные, то в пустой обьект с ошибками ничего не присвоится.

### Слой коммуникации 
#### Класс WebLarekApi

Поля класса:

1) `private api: Api` - с помощью композиции добавили возможность использовать методы и поля экземпляра классса Api. В, котором хранится вся нужная функциональность;
2) `protected data: ObjectApi` - данные, которые приходят с сервера, имеют тип ObjectApi. 

```
type ObjectApi = {
    total: number,
    items: IProduct[]
}
```

Конструктор:
`constructor(private api: Api)` - В конструктор передается экземпляр класса Api. Пишем private, чтобы не инициализировать внутри конструктора, а он автоматически создаст поле класса с переданным из аргумента значением.

Методы: 

1) `getProducts(): Promise<ObjectApi>` - создаем метод класса WebLarekApi на основе get-запроса внутри экзепляра класса Api; Подставляя в дженерик тип, который ждем от сервера. Ждем промис, потому что в классе Api get-запрос возвращает промис.

2) `postProducts(data: PostData): Promise<OrderResponse>` - делаем POST-запрос на сервер и отдаем данные покупателя, имеющие тип PostData и возвращаем промис с типом OrderResponse, который хранит объект с общей суммой покупок и id-Товара;

```
type OrderResponse = {
    total: number,
    id: string;
}
```

```
type PostData = IBuyer & {
    total: number,
    items: string[]
}
```

### Классы представления View

#### Класс Header
Класс для отображения шапки. Расширяет класс компонента.

Конструктор:
`constructor(events: IEvents, container: HTMLElement)` - В конструктор передастся брокер собыйтий и контейнер из component, это тот контейнер, в котором мы будем искать нужные элементы в которых, что-то будет изменяться;

Поля класса:
1) `basketButton: HTMLButtonElement` - поле класса, содержащее кнопку нажатия на корзину, которая находится в шапке;
2) `counterElement: HTMLElement` - поле класса, отображающее число товаров в корзине;

Методы:
1) `set counter(value: number)` - сеттер класса, изменяющего отображение кол-ва товаров в корзине;

#### Интерфейс IHeader 
В параметрах метода render, должны быть те поля, что есть у экземпляра класса;

```
interface IHeader {
  counter: number;
}
```

#### Класс Gallery 
Класс для отображения каталога с карточками товаров на главной странице. Расширяет класс компонента. 

Конструктор:
`constructor(events: IEvents, container: HTMLElement)` - В конструкторе указывается брокер событий и контейнер из Component;

Поля класса:
1) `catalogElement: HTMLElement[]` - поле класса, отображающего каталог товаров в галлереи;

Методы: 
1) `set catalog(items: HTMLElement[])` - сеттер класса, передающий каталог карточек в разметку HTML;


#### Интерфейс IGallery
В параметрах метода render, должны быть те поля, что есть у экземпляра класса;

```
interface IGallery {
  catalog: HTMLElement[];
}
```

#### Класс Modal
Реализует модальное окно. Оно всего одно и по разметке, в него вставляется темплейта другого контента. Расширяет класс компонента. Нужно создать методы открытия и закрытия для управления отображения модального окна. Устанавливает слушатели на клавиатура, для закрытия модального окна по Esc, на клик на оверлей и кнопку крестик на закрытие попапа.

Конструктор:
`constructor(events: IEvents, container: HTMLElement)` - В конструкторе указывается брокер событий и контейнер из Component;

Поля класса:
1) `modalElement: HTMLElement` - поле класса, отвечающего за контейнера `<div class="modal__content"></div>`;
2) `modalButton: HTMLButton` - поле класса, отвечающего за кнопку закрытия у модального окна.

Методы: 
1) `set content(element: HTMLElement)` - сеттер класса, устанавливающий темлэйт внутрь модального окна.
2) `open()` - метод открытия модального окна.
3) `close()` - метод закрытия модального окна

#### Интерфейс IModal 
В параметрах метода render, должны быть те поля, что есть у экземпляра класса;

```
interface IModal {
  content: HTMLElement; 
}
```

#### Класс Order
Класс для отображения успешного заказа пользователя.

Конструктор: 
`constructor(events: IEvents, template: HTMLTemplateElement) ` - В конструкторе указывается брокер событий и контейнер из Component;

Поля класса: 
1) `totalElement: HTMLElement` - поле класса, отображающего кол-во списанных синапсов.
2) `orderButton: HTMLButton` - поле класса, отображающего кнопку заказа.

Методы: 
1) `set total(value: number)` - сеттер для установки отображения общей суммы.

#### Интерфейс IOrder 
В параметрах метода render, должны быть те поля, что есть у экземпляра класса;

```
interface IOrder {
  total: number; 
}
```

#### Класс Сard
Родительский класс с общими для всех карточек функционалом, а также карточки, которые у нас есть, а это карточка в каталоге с доп. информацией, а именно подробным описанием, ценой, категорией и картинкой и кнопкой купить. Карточкой в корзине содержащей порядковый номер и кнопкой удаления из корзины. И карточка в каталоге на главной странице с изображением и категорией. Общий родительский класс будет содержать только общие поля, такие как название карточки и цена.

Поля класса: 
1) `titleElement: HTMLElement` - название карточки, которая есть у всех.
2) `priceElement: HTMLElement` - цена карточки, которая есть у всех.

Конструктор:
`constructor(events: IEvents, container: HTMLElement)` - В конструкторе указывается брокер событий и тимплэйт карточки, которую нужно найти;

Методы класса: 
1) `set title(name: string)` - сеттер для установки названия карточки. 
2) `set price(value: number)` - сеттер для установки цены карточки.

#### Интерфейс ICard 

```
interface ICard {
  title: string;
  price: number;
}
```

#### Класс СardFull 
Родительский класс карточки для подробного отображения внутри него мы находим общие элементы для карточки в модальном окне и карточке каталога. Расширяет класс Card

Конструктор: 
`constructor()` - 

Поля класса: 
1) `categoryElement: HTMLElement` - поле класса, которое будет хранить категорию карточки;
2) `imageElement: HTMLImageElement` - поле класса, которое будет хранить изображение карточки;
 
Методы: 
1) `set category(value: string)` - сеттер класса, который устанавливает категорию у карточки для отображения.
2) `set image(value: string)` - сеттер класса, который устанавливает картинку у карточки для отображения.

#### Интерфейс ICardCatalog 

```
interface ICardCatalog {
  category: string;
  image: string;
}
```

#### Класс CardPreview
Класс карточки для подробного отображения описания карточки. Расширяет родительский класс CardFull.

Конструктор: 
`constructor()` - 

Поля класса: 
1) `buyButton: HTMLButtonElement` - поле класса, хранящее кнопку покупки карточки.
2) `textElement: HTMLElement` - поле класса, хранящее допольнительное описание карточки.

 
Методы: 
1) `set description(value: string)`
2) `set buttonText(value: string)`
3) `set stateButton(value: boolean)` 

#### Интерфейс ICardPreview

```
interface ICardPreview {
  description: string;
  buttonText: string;
  stateButton: boolean;
}
```

#### Класс CardBasket
Класс для отображения карточки в корзине. Расширяет родительский класс карточки. 

Конструктор: 
`constructor()` - 

Поля класса: 
1) `deleteButton: HTMLButtonElement` - поле класса, хранящее кнопку удаления из корзины.
2) `indexElement: HTMLElement` -  поле класса, хранящеее порядковый индекс карточки в корзине.

 
Методы: 
1) `set index(value: number)` -  метод класса, в котором может устанавливать порядковое число для карточки внутри корзины.

#### Интерфейс ICardBasket

```
interface ICardBasket {
  index: number;
}
```

#### Класс BasketView
Класс для отображения корзины.

Конструктор: 
`constructor()` - 

Поля класса: 
1) `listElement: HTMLElement` - 
2) `priceElement: HTMLElement` - 
3) `orderButton: HTMLButtonElement` - 
 
Методы: 
1) `set list(item: HTMLElement[])` -  
2) `set price(value: number)` - 
3) `set stateButton(value:boolean)`-
4) `set stateEmpty(value:string)`-

#### Интерфейс IBasketView

```
interface IBasketView {
  list: HTMLElement[] | null;
  price: number;
  stateButton: boolean;
  stateEmpty: string;
}
```

#### Класс Form
Класс для отображения форм.
Конструктор: 
`constructor()` - 

Поля класса: 
1) `formErrors: HTMLElement`
2) `buttonSubmit: HTMLButtonElement`
 
Методы: 
1) `set errors(value: string)`
2) `set stateButton(value: boolean)`

#### Интерфейс IForm

```
interface IBasketView {
  errors: string
  stateButton: boolean
}
```

#### Класс OrderForm
Класс для отображения формы заказа

Конструктор: 
`constructor()` - 

Поля класса: 
1) `protected formCashButton: HTMLButtonElement`;
2) `protected formCardButton: HTMLButtonElement`;
3) `protected formOrderInput: HTMLInputElement`;
 
Методы: 
1) `set payment(name: string)`
2) `set address(value: string)`

#### Интерфейс IFormOrder
```
interface IFormOrder {
  address: string;
  payment: string;
}
```

#### Класс ContactsForm
Класс для отображения формы контактов

Конструктор: 
`constructor()` - 

Поля класса: 
1)  `protected formInputEmail: HTMLInputElement`;
2)  `protected formInputPhone: HTMLInputElement`;

Методы: 
1) `set email(value: string)`

2) `set phone(value: string)`

#### Интерфейс IContactsForm

```
interface IFormContacts {
  email: string;
  phone: string;
}
```

### Презентер (Логика)
